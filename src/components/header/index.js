import {View, Button, Text} from "react-native";

const Header = () => {
    return(
        <View>
            <Button>Hamburger</Button>
            <Text>Logo</Text>
            <Button>Cart</Button>
        </View>
    )
}

export default Header